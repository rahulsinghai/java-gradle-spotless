rootProject.name = 'java-gradle-spotless'
buildCache {
    local {
        directory = new File('.gradle', 'build-cache')
        removeUnusedEntriesAfterDays = 7
        enabled = true
        push = true
    }
}
